{% extends 'base.html.twig' %}

{% block title %}UserOrder{% endblock %}

{% block body %}
    <h1>UserOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_order.id }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ user_order.filename }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_my_cart_index') }}">back to list</a>

    <a href="{{ path('app_my_cart_edit', {'id': user_order.id}) }}">edit</a>

    {{ include('my_cart/_delete_form.html.twig') }}
{% endblock %}
